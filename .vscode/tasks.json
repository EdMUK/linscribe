{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "xmake: build",
            "command": "xmake",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                ""
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
